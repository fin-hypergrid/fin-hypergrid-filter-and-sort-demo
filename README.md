Combined filtering + sorting demop

This demo combines both the [filtering] and the [sorting] demos, illustrating how to:
* install multiple plugins
* append multiple data transformers to the piped data model
